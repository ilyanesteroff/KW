let divStyle = {
    margin: '0.5vh 1.5%',
    width: '120%',
}

let item = {
    fontSize: '2vh',
    display: 'inline',
    color: 'white',
    fontWeight: '400',
    fontFamily: 'Roboto Condensed, sans-serif',
    cursor: 'pointer'
}

let iconStyle = {
    marginTop: '0',
    marginBottom: '0',
    marginLeft: '1.5vw',
    marginRight: '1vw',
    display: 'inline',
    color: 'yellow',
    fontWeight: '700'
}
 
let upperHeader = {
    backgroundColor: '#332288',
    padding: '0.5vh',
    marginBottom: '0',
    position: 'relative'
}

let lowerHeader = {
    height: '60vh',
    position: 'relative',
    boxShadow: 'inset 0 0 30vh #444444, inset 0 0 30vh #444444',
    opacity: '0.9'
}

let logoImageStyle = {
    position: 'absolute',
    top: '3vh',
    left: '3%'
}

let logoLabelStyle = {
    position: 'absolute',
    top: '6vh',
    left: '9%',
    color: 'white',
    fontFamily: 'Nunito, sans-serif'
}

let navbar = {
    position: 'absolute',
    right: '5%',
    top: '1vh',
    marginTop: '5vh'
}

let navbarItem = {
    color: 'white',
    fontFamily: 'Ubuntu, sans-serif',
    margin: '0 4vw'
}

let rightUpperSection = {
    position: 'absolute',
    right: '2%', 
    top: '0'
}

export { divStyle, item, iconStyle, upperHeader, lowerHeader, logoImageStyle, logoLabelStyle, navbar, navbarItem, rightUpperSection }
